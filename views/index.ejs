<%- include('partials/head.ejs') %>
<body>
	<div id='map'></div>

	<select class="radius-select" name="radius">
		<option value="100">100 m</option>
		<option value="200">200 m</option>
		<option value="300">300 m</option>
		<option value="400" selected>400 m</option>
		<option value="500">500 m</option>
		<option value="600">600 m</option>
		<option value="700">700 m</option>
	</select>

	<h1 class="title">Een stukje <strong>nostalgie</strong></h1>

	<main class="instructions">
		<nav class="pager">
			<ol class="steps">
				<li><a class="slide-link active" href="#slideone">1</a></li>
				<li><a class="slide-link" href="#slidetwo">2</a></li>
				<li><a class="slide-link" href="#slidethree"><%- include('../public/images/icons/finish.svg') %></a></li>
			</ol>
		</nav>

		<section id="slideone" class="slide active">
			<h1>Ga op zoek naar foto's uit het verleden in twee stappen.</h1>
			<p>
				 Ben jij benieuwd hoe jouw straat er vroeger uit zag? Of zou je graag terug blikken op hoe de wijk er ook alweer uit zag? Start met zoeken en laat de herinneringen naar boven komen...
			</p>
			<p>
				Dit project is ontwikkeld door <strong>Max de Vries</strong> en <strong>Suus ten Voorde</strong> in samenwerking met <strong>Adamnet</strong> en de <strong>Openbare Bibliotheek Amsterdam.</strong>
			</p>
		</section>

		<section id="slidetwo" class="slide">
			<h1>Kies een locatie</h1>
			<h2>Verplaats de cirkel om een gebied te selecteren...</h2>
			<p>
				Denk bijvoorbeeld aan een wijk waar je hebt gewoond, een bekende plek in de stad of het park waar je veel te vinden was.
			</p>
			<h2>...of ga op zoek naar een straat.</h2>
			<p>
				Denk bijvoorbeeld aan een woonadres, de straat van jouw school of een straat van een vriend of vriendin.
			</p>

			<form class="searchbar" action="/search-location" method="post">
				<input type="text" name="searchLocation" placeholder="Zoek jouw straat..." value="" autocomplete="off">
				<button type="submit">
					<%- include('../public/images/icons/search.svg') %>
				</button>
			</form>

			<div class="autocomplete">
				<% if (data !== null && data !== undefined) { %>
					<ul id="autocomplete-list" class="autocomplete-items">
						<% data.forEach((street, i) => { %>
							<li>
								<label for="<%- street.street.value %>"><%= street.name.value %></label>
								<input id="<%- street.street.value %>" type="radio" name="wkt" value="<%- street.wkt.value %>">
							</li>
						<% }); %>
					</ul>
				<% } %>
			</div>
		</section>

		<section id="slidethree" class="slide">
			<h1>Kies een periode</h1>
			<h2>Selecteer een beginjaar en een eindjaar.</h2>
			<p>
				Denk bijvoorbeeld aan beelden tijdens de Tweede Wereldoorlog. Kies dan als beginjaar 1940 en eindjaar 1945.
			</p>

			<form action="" method="post">
				<% var now = new Date(Date.now()).getFullYear(); %>

				<ul class="year-selection">
					<li>
						<h2>Beginjaar:</h2>
						<small>(Vanaf 1900)</small>
						<select class="startyear">
							<% var i = 1900; %>
							<% while (i < now+1) { %>
								<% if (i == 1970) { %>
									<option value="<%- i %>" selected><%= i %></option>
								<% } else { %>
									<option value="<%- i %>"><%= i %></option>
								<% } %>
								<% i++; %>
							<% } %>
						</select>
					</li>
					<li>
						<h2>Eindjaar:</h2>
						<select class="endyear">
							<% var i = 1900; %>
							<% while (i < now+1) { %>
								<% if (i == 1980) { %>
									<option value="<%- i %>" selected><%= i %></option>
								<% } else { %>
									<option value="<%- i %>"><%= i %></option>
								<% } %>
								<% i++; %>
							<% } %>
						</select>
					</li>
				</ul>
			</form>
		</section>

		<a class="action-btn action-btn--white slide-link" href="#slidetwo" role="button">
			<span>Naar stap 2</span>
			<%- include('../public/images/icons/arrow-right.svg') %>
		</a>
	</main>
<%- include('partials/foot.ejs') %>
